[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "factor-strategy-agent"
version = "0.2.0"
description = "factor_strategy_agent: papers → factors → strategy toolbox + empirical knowledge base."
requires-python = ">=3.10"
dependencies = [
  "azure-ai-formrecognizer",
  "dill",
  "docker",
  "filelock",
  "fire",
  "fuzzywuzzy",
  "jqdatasdk",
  "langchain-community",
  "litellm>=1.73",
  "loguru",
  "networkx",
  "numpy",
  "openai",
  "pandarallel",
  "pandas",
  "pyarrow",
  "pydantic-ai-slim[mcp,openai,prefect]",
  "pydantic-settings",
  "pyyaml",
  "psutil",
  "pymupdf",
  "pypdf",
  "python-dotenv",
  "python-Levenshtein",
  "requests",
  "rich",
  "scikit-learn",
  "scipy",
  "tables",
  "tiktoken",
  "tqdm",
  "tushare",
  "typer",
]

[project.scripts]
start = "factor_strategy_agent_cli.main:start_main"
fsa = "factor_strategy_agent_cli.main:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["factor_strategy_agent_cli*", "paper_factor_cli*", "rdagent*"]

[tool.setuptools.package-data]
rdagent = ["**/*.yaml", "**/*.md", "**/*.txt", "**/Dockerfile"]
